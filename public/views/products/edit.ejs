<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Editar productos de Productos</h1>
    <div>
        <form action="/products/edit/<%= product.id %>" method="POST">
            <label for="name">Nombre:</label> <br>
            <input type="text" name="name" value="<%= product.name %>"  autofocus required > <br>
            <label for="description">Descripción:</label> <br>
            <input type="text" name="description"  value="<%= product.description %>" required> <br>
            <label for="category_id">Categoria: </label> <br>
            <select name="category_id" id="" value="Elige Categoria">
                <% for(let i = 0; i < h.length; i++){ %>
                    <option value="<%= h[i].id %>"><%= h[i].name %> </option>
                <% } %>
            </select> <br> <br>
            <button>
                Editar
            </button>
        </form>
    </div>
    <a href="/products">Ver productos</a>
</body>
</html>