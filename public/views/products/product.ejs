<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Products Page</h1>
    
    <div>
        <% if (products.length === 0) { %>
            <h2>No existe producto registrado</h2>
        <% } %> 

        <% for(var i=0; i<products.length; i++){ %>
            <a href=""> <h1><%= products[i].name %></h1> </a>   
            <h3><%= products[i].description %></h3>
            <h4><%= products[i].category_id %></h4>
            
            <a href="/products/delete/<%= products[i].id %>">
                <button>Eliminar Producto</button>
            </a>
            <a href="/products/edit/<%= products[i].id %>">
                <button>Editar Producto</button>
            </a>       
            <% } %>
    </div>
    <br>
    <a href="/products/add">Registrar nuevo producto</a> <br>
    
    <a href="/category">Categorias</a>
</body>
</html>